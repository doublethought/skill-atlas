# Optional: add a PostgreSQL connection string for persistent data.
# Without this value, Skill Atlas runs with an in-memory demo dataset.
DATABASE_URL="postgresql://user:password@host:5432/database"

# Optional: make the frontend run as a read-only public demo.
# This is useful for static hosting on Vercel.
VITE_DEMO_MODE="readonly"
